```
S = serverinfo
s = systeminfo
U = userinfo (gets sent to server)
R = read only
I = init (can't be changed after game is loaded, only on command line)
A = archive (saved to config file)
L = latch (won't change until next map)
C = cheat
? = unknown (manually created with set command)
```
