
//# This file is loaded first on every com_init()
//  then q3config.cfg
//  then autoexec.cfg

//# Remove qvm and allow dll loading
//   they are default in osdf-engine, but q3 defaults overwrite them
seta vm_cgame 0
seta vm_game 0
seta vm_ui 0
seta sv_pure 0
